# Power Automate monitoring

Use `scan_inventory`, Store summaries and `get_live_flow_runs` for bounded metadata monitoring. Use `get_live_flow_run_summary` for a selected run-level error and label it as incomplete without runtime action details. Do not persist raw payloads or represent local governance labels as Microsoft-native alerts, DLP, ownership or audit records.
